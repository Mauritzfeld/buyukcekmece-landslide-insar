# Python dependencies for generate_figures.py
#
# Tested with Python 3.9 and the pinned versions below. The upper bound on
# pandas is required: generate_figures.py relies on pd.to_datetime(...,
# errors='ignore') to detect the spreadsheet date columns, which is removed in
# pandas 3.0.

numpy>=1.23,<3
pandas>=1.5,<3
matplotlib>=3.6
scipy>=1.9
geopandas>=0.12
shapely>=2.0
geopy>=2.2
fiona>=1.9
pyproj>=3.4
contextily>=1.3
openpyxl>=3.0          # required by pandas to read the .xlsx inputs
